[package]
name = "turbo-core"
version = "0.1.0"
edition = "2024"

build = "build/main.rs"

[build-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
anyhow = "1.0.104"

[dev-dependencies]
rstest = "0.26.1"
